# CLAUDE.md (user level)

- **Git: confirm before acting.** Always show me the exact git commands (commit message, push target, rebase, etc.) and wait for my confirmation before running anything that changes git state. Read-only git commands (status, log, diff) are fine without asking.
